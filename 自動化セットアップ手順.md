# 寿司職人ナビ 自動化セットアップ手順

## 1. Anthropic APIキーを取得

1. https://console.anthropic.com/ にアクセス
2. ログイン → 「API Keys」→「Create Key」
3. キーをコピー（sk-ant-... で始まる文字列）

---

## 2. 毎日自動投稿の設定

### 手順① daily_post.bat を編集

`daily_post.bat` を開いて、APIキーを設定：

```
set ANTHROPIC_API_KEY=sk-ant-ここにキーを入力
```

### 手順② タスクスケジューラーに登録

`setup_scheduler.bat` を**右クリック→管理者として実行**

→ 毎日09:00に自動で記事を生成してGitHub pushされます

### 手順③ 動作確認

```
cd C:\Users\81909\sushi-blog
set ANTHROPIC_API_KEY=sk-ant-...
python auto_post.py
```

---

## 3. noteサブスクコース記事の生成

### Week1・2 は既に作成済み
`note-articles/` フォルダに保存されています。

### Week3〜12 を生成する

```
cd C:\Users\81909\sushi-blog
set ANTHROPIC_API_KEY=sk-ant-...
python note_course_generator.py 3 4 5   # Week3〜5だけ生成
python note_course_generator.py          # 全部一気に生成
```

---

## 4. noteへの投稿手順

1. note.com にログイン
2. 「マガジン」→「新しいマガジンを作成」
3. 設定：
   - マガジン名: **寿司職人転職塾**
   - 説明: 未経験から寿司職人に転職するための全12回の実践コース
   - 価格: **月額980円（定期購読）**
4. Week1（week01_self_analysis.md）を投稿 → **無料・全文公開**
5. Week2以降を順番に投稿 → **有料（マガジン購読者限定）**

---

## 5. ブログ記事のトピックを追加

`article_topics.json` の `pending` 配列に追加：

```json
{
  "slug": "記事のURLに使う英単語",
  "title": "記事タイトル",
  "keywords": ["SEOキーワード1", "キーワード2"]
}
```

---

## ファイル一覧

| ファイル | 役割 |
|---------|------|
| `auto_post.py` | ブログ記事の自動生成・投稿 |
| `article_topics.json` | 投稿予定のトピックリスト |
| `note_course_generator.py` | noteコース記事の生成 |
| `daily_post.bat` | 毎日実行するバッチ（APIキー設定済み） |
| `setup_scheduler.bat` | タスクスケジューラーへの登録 |
| `auto_post_log.txt` | 自動投稿のログ |
| `note-articles/` | note投稿用の記事ファイル |
